CREATE TABLE mybatis_db.cars
(
    id int(11) NOT NULL AUTO_INCREMENT,
    brand varchar(100) NOT NULL,
    productionDate varchar(100) NOT NULL,
    numberOfDoors int(4) NOT NULL,
    PRIMARY KEY (id)
);

